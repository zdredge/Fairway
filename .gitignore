# Dependencies
node_modules/

# SvelteKit / Vite build output
/.svelte-kit/
/build/
/dist/
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Environment — keep the example, never the real values
.env
.env.*
!.env.example

# Local SQLite database (incl. WAL sidecar files)
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor noise
.DS_Store
Thumbs.db

# Claude Code personal settings (launch.json is shared; local settings are not)
.claude/settings.local.json
